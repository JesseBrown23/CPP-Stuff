#include <iostream>
#include <cstdlib>

using namespace std;

//Prototyping fatorial
int factorial(int x);

int main()
{

    cout << factorial(5) << endl;

    return 0;
}


int factorial(int x){
    if(x==1) {
        return 1;
    }else{
        return x*factorial(x-1);
    }
}
